# 102 vs 201 · 单屏策略对战 — 产品需求文档（PRD，含原型设计）

> 一句话定位：部署在 `x102201.github.io` 首页的「102 vs 201」单屏策略对战小游戏——用 **1 分钟一局、一屏完成、永不滚动** 的对决，把访客变成玩家。

---

## 1. 文档信息

| 项 | 内容 |
| --- | --- |
| 文档名称 | 102 vs 201 · 单屏策略对战 PRD（含原型设计） |
| 版本 | v1.0 |
| 状态 | 评审稿 |
| 日期 | 2026-08-18 |
| 负责人 | x102201 |
| 关联仓库 | https://github.com/x102201/x102201.github.io （GitHub Pages 个人主页） |
| 关联文件 | `index.html` / `style.css` / `game.js` / `README.md` |

---

## 2. 产品概述

### 2.1 背景

个人主页 `x102201.github.io` 需要一件"留客"的互动作品：访客打开首页后，不应只是看一眼就走，而应**玩上一局、记住这个页面、明天还想再来**。本游戏以账号名天然分裂出的两个阵营 **102营（蓝）** 与 **201营（橙）** 为世界观核心，借"田忌赛马"的策略内核，做成一款双方对战的小游戏。

### 2.2 产品形态

- 纯前端小游戏，嵌入 GitHub Pages 首页，**无后端、无登录、无外链依赖**。
- **单屏舞台**：整个游戏只占一个视口（一屏），任何状态下页面**都不出现滚动**。
- 单局时长：约 60~90 秒（含动画），可"快进"压缩至约 20 秒。

### 2.3 产品边界（明确不做）

| 不做 | 原因 |
| --- | --- |
| 在线多人对战 | 纯静态托管无后端，无法实时同步 |
| 账号/注册/云端存档 | 无后端；本地 `localStorage` 存档即可 |
| 内购/广告 | 个人主页作品，保持纯净 |
| 多语言 | 首版仅简体中文 |

---

## 3. 目标与成功指标

| 目标 | 指标（可埋点或人工观测） |
| --- | --- |
| 访客→玩家转化 | 打开首页后点击「开始对决」的比例 ≥ 40% |
| 一局玩得完 | 单局完成率（开赛→结算）≥ 90% |
| 停留更久 | 平均会话时长 ≥ 3 分钟（多局连玩） |
| 明天还来 | 每日挑战完成率：同设备次日回访 ≥ 20% |
| 有谈资 | 战绩/成就可截图分享（P2 落地分享卡片） |

---

## 4. 用户与场景

### 4.1 目标用户

| 画像 | 特征 |
| --- | --- |
| 技术访客 | GitHub 用户、开发者，喜欢"有点子"的作品，易被彩蛋/梗吸引 |
| 碎片玩家 | 通勤/排队时玩 1~2 局，单局必须短平快 |
| 反复回流者 | 被段位、连胜、每日挑战驱动的轻度收集党 |

### 4.2 核心场景

1. 访客打开主页 → 10 秒内看懂"102 vs 201 对决" → 点「开始对决」。
2. 选阵营（102 蓝 / 201 橙）→ 秘密布阵 → 开赛 → 三场动画 → 结算。
3. 输了不服气 → 「再来一局」；赢了想炫耀 → 截图分享（P2）。
4. 第二天打开 → 「每日挑战」换新规则 → 再玩一局。

---

## 5. 核心玩法设计

### 5.1 世界观与术语

裁判 **X**（紫色）主持 102营 与 201营 的对决。X 既当主持人，也当"会学习的对手"。

| 术语 | 说明 |
| --- | --- |
| 102营 / 201营 | 玩家可选的双方阵营（蓝/橙），外观不同、数值完全公平 |
| 裁判 X | AI 对手兼主持人；段位越高，X 越会记住玩家阵型并反制 |
| 马匹 | 上等马（速 3）、中等马（速 2）、下等马（速 1），每方各一匹 |
| 赛道 | 3 条赛道，按 第1场 / 第2场 / 第3场 出战 |
| 布阵 | 把 3 匹马秘密安排到 3 条赛道（共有 6 种排列） |
| 声望 / 段位 | 胜负积分的成长体系，青铜 → 最强军师 |
| 每日挑战 | X 每天定一条特殊规则，胜了拿双倍奖励 |

### 5.2 基本规则（P0 核心，沿用现有实现并文档化）

1. 双方各 3 匹马：🦄 上等马（速 3）> 🐎 中等马（速 2）> 🐴 下等马（速 1）。
2. 双方**秘密**布阵到 3 条赛道；开赛后逐场揭晓、同时起跑。
3. 单场胜负：速度快者胜；**同速由裁判 X 掷硬币**裁决。
4. 三局两胜：先赢两场者赢得整局。
5. 经典解法：田忌阵「下→上→中」——下等马对对方上等马（输 1 场），上等马对对方中等马、中等马对对方下等马（赢 2 场），以弱胜强。
6. 随机因素：对方阵型随机、同速掷硬币——保证每局都有悬念；但策略（布阵）始终是胜负的主导因素。

### 5.3 对战模式（双模式）

| 模式 | 说明 | 优先级 |
| --- | --- | --- |
| **单人对战（vs 裁判 X）** | 玩家选阵营，与 AI 对局；AI 会随玩家胜场增多而学会反制（见 5.4.4）。含全部成长/存档系统 | **P0 主模式** |
| **本地双人热座（102 vs 201）** | 同一台设备两人对战：102 营先秘密布阵 → 设备交给 201 营秘密布阵 → 同时揭晓开赛。判定共用同一套规则；不计声望、不触发 X 学习 | **P1 扩展**（原型已在 7.3 预留入口位） |

> 设计说明：在线双人在纯静态站上不可行，故"双方对战"以**本地热座**实现——朋友聚会场景下"把手机递给对方"本身就是很好的传播点。

### 5.4 成长与留存系统（P0 全量保留，沿用现有实现）

#### 5.4.1 声望与段位
胜利 +10 声望（每日挑战胜利 +20），失败 +3 参与分；附加分：完胜 3:0 +5、连胜≥3 +2、忠诚加成 +2、每日首胜 +10、双倍日 ×2。段位共 6 级（见 9.2）。

#### 5.4.2 连胜与战报
连胜数 + 最近 8 局胜负点（`recent`），主页数据条常驻展示。

#### 5.4.3 成就（12 项）
首战告捷、三连胜、五连胜、田忌传人、完胜、大师克星、常胜将军、百战之师、每日军师、出其不意、绝地翻盘、忠诚卫士（详见 9.4）。

#### 5.4.4 会学习的 X（反制机制）
X 有 4 级（新晋/正式/资深/传奇裁判，随玩家胜场升级）。等级越高，X 越可能用「最优反制阵型」应对玩家上一场阵型——**逼玩家不断变阵**，形成策略深度。

#### 5.4.5 每日挑战（回流钩子）
按日期取模从 6 种规则中轮换（迷雾日/顺风日/逆风日/混沌日/强敌日/双倍日，见 9.3），每天不同，次日回访理由。

---

## 6. 核心设计原则：单屏 · 无滚动

> 这是本产品的**第一硬性约束**，优先级高于视觉表现。任何状态、任何视口、任何动画，都不允许页面出现滚动。

### 6.1 硬性约束（验收级）

| 编号 | 约束 |
| --- | --- |
| **R1** | 任意受支持视口（最小 320×480）下，`scrollHeight ≤ innerHeight` 且 `scrollWidth ≤ innerWidth`，不出现任何滚动条、不触发任何滚动 |
| **R2** | 五个状态（主页 / 选阵营 / 布阵 / 对局 / 结算）在**同一个视口内**完成切换，切换过程零滚动位移 |
| **R3** | 所有弹窗 / 覆盖层不增加文档高度；弹窗内容超高时**仅弹窗内部**滚动，页面不动 |
| **R4** | 对局动画（赛马、金币、横幅、彩带）必须在舞台范围内完成，不溢出、不撑高 |

### 6.2 实现策略

#### 策略 A：固定逻辑画布 + 等比缩放（主方案，保证 R1 恒成立）

- 逻辑画布固定为 **420 × 800**（手机竖屏优先；桌面/横屏按比例放大后居中显示，画布外留白由背景星空填充）。
- 全局缩放变量：

```css
:root { --S: min(100vw / 420, 100vh / 800); }
#stage {
  width: 420px; height: 800px;
  transform: scale(var(--S));
  transform-origin: center;
  overflow: hidden;
}
```

- 舞台内部全部使用**逻辑像素**书写尺寸，字号下限 13px、可点目标 ≥ 44×44，保证缩放到最小视口（320×480，缩放比 ≈ 0.6）时内容仍完整可见、可操作。
- 舞台用 `display:grid; place-items:center` 包裹，在任何视口内水平垂直居中；缩放导致的轻微模糊可接受（霓虹风格下不明显）。

#### 策略 B：禁止滚动的一切触发点

- `html, body { height: 100%; overflow: hidden; }`。
- **删除**现有 `scrollIntoView` / `window.scrollTo` 调用（现有 `game.js` 的 `scrollToId` / `scrollTop` 全部移除，状态切换改为层内切换，见策略 C）。
- 弹窗用 `position: fixed` 居中，不参与文档流。

#### 策略 C：状态图层化（R2 的实现）

- 赛场主区 `.screen` 为 `position: relative` 容器，内部叠放**状态层**：主页空状态、选阵营层、布阵层、对局层、结算层，均为 `position: absolute; inset: 0`。
- 同一时刻只有一层 `.active`；切换 = 层内 300~400ms 淡入淡出/位移动画，**不改变任何外部容器高度**。
- 顶栏、英雄条、数据条为**常驻层**，任何状态下可见（玩家随时能看段位、关声音、看规则）。

#### 策略 D：安全区与刘海屏

- 舞台外层留 `env(safe-area-inset-*)` 安全边距；`viewport-fit=cover`。

### 6.3 与现有实现的差距（P0 改造清单）

| # | 现状（会滚动） | 目标（单屏无滚动） |
| --- | --- | --- |
| 1 | 备战区 `.phase` 用 `grid-template-rows` 展开，**撑高文档** | 改为舞台内绝对定位覆盖层 |
| 2 | `game.js` 调用 `scrollIntoView` / `scrollTo`（`scrollToId` / `scrollTop`） | 全部移除，切换走图层 |
| 3 | 结算区以文档流滑出，位于页面底部 | 改为覆盖在赛场主区上的结算层 |
| 4 | 无整体缩放，小屏必然溢出 | 引入 `#stage` 逻辑画布 + `--S` 缩放 |
| 5 | 弹窗内容超高时依赖页面滚动 | 弹窗自身 `max-height` + 内部滚动 |

---

## 7. 原型设计（本 PRD 核心交付）

> 以下线框均为同一视口框架内的不同**状态**。约定：`[ ]` 为按钮/卡片，`(X)` 为裁判 X 徽章，`●/○` 为战报点，`▏` 为终点线。线框仅表达结构与层级，非最终视觉。

### 7.1 图 0：单屏舞台总框架

```
┌────────────────────────────────────────────┐
│ 顶栏   102✕201          [段位] [音效] [规则]│ ← 固定高 44px
├────────────────────────────────────────────┤
│ 英雄条  (X) 我是裁判X，规则我来定！         │ ← 固定高 ~100px
│        102 ── VS ── 201                    │
│        田忌赛马 · 三场对阵 · 以弱胜强       │
├────────────────────────────────────────────┤
│ 数据条  [声望] [段位] [连胜] [总胜场]       │ ← 固定高 ~120px
│        最近战报 ● ● ○ ○ ○ ○ ○ ○           │
│        [ 开始对决 ] [ 每日挑战 ]            │
├────────────────────────────────────────────┤
│ 赛场主区 .screen（relative · flex:1        │ ← 弹性，占剩余高度
│  · min-height:0）                           │    （800-44-100-120=536px）
│  ┌──────────────────────────────────────┐  │
│  │   状态层 A/B/C/D/E 在此区域内切换     │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
  （背景：星空粒子 + 漂移的 1/0/2 字符，全屏铺底，不占布局）
```

### 7.2 图层关系与状态机

```
#stage（420×800 逻辑画布，overflow:hidden）
├─ 常驻层（文档流，不参与状态切换）
│   ├─ 顶栏 / 英雄条 / 数据条
│   └─ 赛场主区 .screen（position:relative）
│       ├─ 状态层 A·主页（默认 active）
│       ├─ 状态层 B·选阵营（absolute 覆盖）
│       ├─ 状态层 C·布阵（absolute 覆盖）
│       ├─ 状态层 D·对局（absolute 覆盖）
│       └─ 状态层 E·结算（absolute 覆盖）
├─ 特效层（不占布局，absolute）：回合横幅 / 金币 / 彩带 / Toast
└─ 全局弹窗（fixed）：成就殿堂 / 玩法规则 / 每日挑战
```

```
            [开始对决]      [加入102营/201营]
  主页 ────────────────▶ 选阵营 ──────────▶ 布阵
   ▲                     ▲                  │ [开赛！]
   │ [回主页]            │                  ▼
   │ ◀───────────────────┴─────────── 对局（3 场动画）
   │                                        │
   │ [回主页]                               ▼
   └────────────────────────────────── 结算 ──[再来一局]──▶ 布阵
                                          │
                                          └──[换阵营]──▶ 选阵营
                                          （热座入口：主页数据条 [👥 双人对战]，P1）
```

### 7.3 五状态线框图

**图 1 · 状态 A 主页（默认）**

```
┌────────────────────────────────────────────┐
│ 102✕201            🥉青铜军师  🔊  📖     │
├────────────────────────────────────────────┤
│  (X) 我是裁判X，规则我来定！               │
│  102 ── VS ── 201                          │
│  田忌赛马 · 三场对阵 · 以弱胜强            │
├────────────────────────────────────────────┤
│  [声望 0] [🥉青铜军师] [🔥连胜 0] [0胜0负] │
│  最近战报 ○ ○ ○ ○ ○ ○ ○ ○                 │
│  [ 开始对决 ]  [ 每日挑战·迷雾日 ]         │
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐  │
│  │                                      │  │
│  │        X 正等待挑战者入场……          │  │
│  │      （赛场空状态 + 轻微入场动画）    │  │
│  │                                      │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

**图 2 · 状态 B 选阵营（覆盖赛场主区；顶栏/英雄条/数据条常驻）**

```
┌────────────────────────────────────────────┐
│ 102✕201            🥉青铜军师  🔊  📖     │
├────────────────────────────────────────────┤
│  (X) 选哪边都行，X只负责公平！             │
│  102 ── VS ── 201                          │
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐  │
│  │           选 择 你 的 阵 营          │  │
│  │  ┌─────────────┐ ┌─────────────┐    │  │
│  │  │   (蓝)      │ │   (橙)      │    │  │
│  │  │   102营     │ │   201营     │    │  │
│  │  │ 正着念，蓝方│ │ 倒着念，橙方│    │  │
│  │  │ 必胜        │ │ 无敌        │    │  │
│  │  │ [加入102营] │ │ [加入201营] │    │  │
│  │  └─────────────┘ └─────────────┘    │  │
│  │  [ ← 回主页 ]                       │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

**图 3 · 状态 C 布阵（覆盖赛场主区）**

```
┌────────────────────────────────────────────┐
│ 102✕201            🥉青铜军师  🔊  📖     │
├────────────────────────────────────────────┤
│  (X) 今日规则：迷雾日 🌫️ · X已记住你上一场…│
│  102营 · 排兵布阵                          │
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐  │
│  │  第1场  [ 槽位：点击下方马匹放入 ]   │  │
│  │  第2场  [ 槽位：点击下方马匹放入 ]   │  │
│  │  第3场  [ 槽位：点击下方马匹放入 ]   │  │
│  │  ──────────────────────────────────  │  │
│  │  我的马匹（点击 → 放入赛道）         │  │
│  │  [🦄 上等·速3] [🐎 中等·速2]        │  │
│  │  [🐴 下等·速1]                      │  │
│  │  [ 随机布阵 ] [ 田忌妙计 ] [ 清空 ] │  │
│  │  [ ← 回主页 ]       [ 开赛！ ]      │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

**图 4 · 状态 D 对局（覆盖赛场主区；动画均在舞台内）**

```
┌────────────────────────────────────────────┐
│ 102✕201            🥉青铜军师  🔊  📖     │
├────────────────────────────────────────────┤
│  [102营 0] ── [201营 0]    ● ○ ○ [快进]   │
│  (X) 第1场：小蓝(下等) vs 小红(下等)！放马！│
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐  │
│  │ 102营·第1场  🐴小蓝 速1 ──────▶  ▏终点│  │
│  │ 201营·第1场  🐴小红 速1 ──────▶  ▏终点│  │
│  │ ──────────────────────────────────   │  │
│  │ 第1场 [🦄上等] VS [🐎中等]    [胜]   │  │
│  │ 第2场 [🐎中等] VS [🐴下等]    […]    │  │
│  │ 第3场 [🐴下等] VS [🦄上等]    […]    │  │
│  └──────────────────────────────────────┘  │
│  （同速时：金币动画覆盖赛道区；             │
│    回合横幅覆盖赛道区，均 absolute 不撑高） │
└────────────────────────────────────────────┘
```

**图 5 · 状态 E 结算（覆盖赛场主区）**

```
┌────────────────────────────────────────────┐
│ 102✕201            🥉青铜军师  🔊  📖     │
├────────────────────────────────────────────┤
│  (X) 本场对决，X宣布你获胜！               │
├────────────────────────────────────────────┤
│  ┌──────────────────────────────────────┐  │
│  │              🏆 胜 利 ！             │  │
│  │               2 : 1                  │  │
│  │  X：三局两胜，田忌式翻盘！           │  │
│  │  +12 声望：胜利+10 · 连胜加成+2      │  │
│  │  🔥 当前连胜 3 场 · 最佳 5 场        │  │
│  │  我方 102营：🦄上 → 🐎中 → 🐴下      │  │
│  │  对方 201营：🐎中 → 🐴下 → 🦄上      │  │
│  │  [ 再来一局 ]  [ 换阵营 ]            │  │
│  │  [ 回主页 ]   [ 📤 分享战绩（P2）]   │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

### 7.4 关键交互清单

| 状态 | 控件 | 行为 |
| --- | --- | --- |
| 主页 | [开始对决] | 未选阵营→切到选阵营层；已选→直接进布阵层 |
| 主页 | [每日挑战] | 弹窗展示今日规则；[接受挑战]→进布阵层（daily 模式） |
| 主页 | [成就]/[玩法]/顶栏📖 | 全局弹窗（fixed，内部滚动） |
| 选阵营 | [加入102营]/[加入201营] | 存档阵营 → 进布阵层；热座模式下为 [我是102营]（P1） |
| 布阵 | 马池卡片 / 赛道槽位 | 点马入槽 / 点槽取回；槽满后 [开赛！] 可点 |
| 布阵 | [随机布阵]/[田忌妙计]/[清空] | 一键预设，即时刷新层内内容 |
| 对局 | [快进] | 动画速度 ×3（最短约 20 秒一局），可随时切换 |
| 结算 | [再来一局]/[换阵营]/[回主页] | 层内切换：→布阵 / →选阵营 / →主页 |
| 全局 | 顶栏 🔊 / 📖 | 音效开关 / 玩法规则弹窗 |
| 全局 | Esc / 点遮罩 | 关闭弹窗 |

---

## 8. UI 规格

### 8.1 视觉体系（沿用现有 CSS 变量）

| 项 | 规格 |
| --- | --- |
| 主题 | 暗夜霓虹（深蓝底 `#060a1c`/`#0b1026` + 玻璃拟态卡片） |
| 阵营色 | 102 蓝 `#38bdf8` / 201 橙 `#fb923c`（含渐变 `-2` 变量） |
| 裁判 X | 紫 `#e879f9`，用于 VS、徽章、金币、横幅 |
| 点缀 | 金色 `#fbbf24`（声望/速度徽标）、绿/红（胜/负） |
| 字体 | 标题/数字：`Space Mono`；正文：系统中文栈（PingFang/YaHei） |
| 背景 | Canvas 星空粒子 + 漂移 `1/0/2` 字符，全屏铺底（现有 `#bg`） |

### 8.2 文字与间距

- 舞台内逻辑像素：基础字号 13~16px，标题 20~40px，间距 8/12/16 三级。
- 窄屏压缩策略：统计卡 4→2 列、隐藏次要文字、字号随 `clamp()` 收缩——**优先于**缩放兜底，保证常规视口（≥360×640）字高 ≥ 11px 物理像素。
- 可点目标：主按钮 ≥ 44×44 逻辑像素。

### 8.3 动效与反馈

| 场景 | 反馈 |
| --- | --- |
| 点击 | WebAudio 合成短音（click） |
| 开赛 | 哨声 + 马蹄声（`SFX.gallop`） |
| 同速裁决 | 金币旋转动画 + 叮声 |
| 胜负 | 回合横幅（win/lose/tie 三色）+ 结算彩带 |
| 成就解锁 | Toast 提示 + 音效 |

动效时长纪律：层切换 ≤ 400ms；单场动画 ≤ 2.5s（快进 ≤ 1s）；结算 500ms 内呈现。

### 8.4 无障碍

- 裁判 X 气泡、Toast 加 `aria-live="polite"`；按钮均带可读文案。
- 弹窗打开时焦点移入首个控件，关闭后归还焦点；Esc 关闭。
- 对比度：正文白字对深底 ≥ 4.5:1（现有配色满足）。

---

## 9. 数据与存档

### 9.1 localStorage Schema（沿用键 `tj102201_v1`）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `camp` | `'102' \| '201' \| null` | 当前阵营 |
| `lastCamp` | 同上 | 上一局阵营（忠诚加成判定） |
| `points` | number | 声望 |
| `wins` / `losses` / `matches` | number | 累计战绩 |
| `streak` / `bestStreak` | number | 当前连胜 / 最佳连胜 |
| `campWins` | `{102:n, 201:n}` | 各阵营胜场（忠诚卫士成就） |
| `recent` | `('w'\|'l')[]`，≤8 | 最近战报 |
| `ach` | `{id:true}` | 已解锁成就 |
| `flags` | `{tianji,perfect,beatMaster,surprise,comeback}` | 条件成就旗标 |
| `lastPlayerArr` | `tier[3]` | 上一场阵型（X 学习反制用） |
| `dailyDate` / `dailyDone` / `dailyDoneCount` | string / bool / number | 每日挑战状态 |
| `sound` | bool | 音效开关 |

### 9.2 段位表

| 段位 | 所需声望 |
| --- | --- |
| 🥉 青铜军师 | 0 |
| 🥈 白银军师 | 120 |
| 🥇 黄金军师 | 320 |
| 💠 铂金军师 | 680 |
| 💎 钻石军师 | 1200 |
| 👑 最强军师 | 1900 |

### 9.3 每日规则表（按日期取模轮换）

| 图标 | 名称 | 规则 |
| --- | --- | --- |
| 🌫️ | 迷雾日 | 第 2 场强弱颠倒：低级马获胜 |
| 🌬️ | 顺风日 | 玩家第 3 场马匹速度 +1 |
| 🌀 | 逆风日 | 玩家第 1 场马匹速度 -1 |
| 🎲 | 混沌日 | 每场都由 X 掷硬币决定 |
| 💪 | 强敌日 | 对方第 3 场马匹速度 +1 |
| 💰 | 双倍日 | 本日获胜积分翻倍 |

### 9.4 成就表（12 项）

首战告捷 / 三连胜 / 五连胜 / 田忌传人（经典阵获胜）/ 完胜（3:0）/ 大师克星（胜传奇裁判）/ 常胜将军（累计 10 胜）/ 百战之师（累计 100 局）/ 每日军师（完成一次每日挑战）/ 出其不意（换阵后立刻获胜）/ 绝地翻盘（首场失利后逆转）/ 忠诚卫士（同阵营 10 胜）。

---

## 10. 技术约束

| 约束 | 说明 |
| --- | --- |
| 技术栈 | 纯 HTML / CSS / 原生 JS（ES5 风格，零构建、零依赖） |
| 托管 | GitHub Pages 静态托管；直接双击 `index.html` 亦可离线运行 |
| 存档 | `localStorage`（同源），无网络请求 |
| 音效 | WebAudio 合成（无音频文件，首帧用户手势后解锁） |
| 动画 | Canvas（星空/彩带）+ CSS transition（赛道/图层） |
| 浏览器 | 现代 Chrome/Edge/Safari/Firefox 及移动端 WebView |
| 响应式 | 仅依赖上面 6.2 的缩放策略，不做断点式重排 |

---

## 11. 验收测试用例

### 11.1 单屏 · 无滚动专项（阻断级）

| # | 步骤 | 预期 |
| --- | --- | --- |
| T1 | 在 320×480、375×667、414×896、1280×800、1920×1080 视口下打开首页 | 无滚动条；`scrollHeight ≤ innerHeight`、`scrollWidth ≤ innerWidth` |
| T2 | 依次走完 主页→选阵营→布阵→对局→结算，再回主页 | 全程页面零滚动；每次切换仅层内动画 |
| T3 | 对局中触发同速掷硬币、回合横幅 | 金币/横幅在舞台内完整显示，不溢出视口 |
| T4 | 结算后点[再来一局]直接再开 | 无跳动、无滚动、层切换 ≤400ms |
| T5 | 打开成就/玩法/每日弹窗 | 弹窗固定居中；内容超高仅弹窗内部滚动，页面不动 |
| T6 | 最小视口 320×480 下操作完整一局 | 所有按钮可点、文字可读、无裁切 |

### 11.2 玩法回归

| # | 步骤 | 预期 |
| --- | --- | --- |
| T7 | 三场全胜 / 2:1 / 0:3 | 结算比分与胜负正确，声望按 9.1 规则累计 |
| T8 | 布上下等 vs 对方上等（同速） | 出现硬币裁决且结果有效 |
| T9 | 连赢 2 局后同阵型再战 | X 等级提升后开始反制（胜率下降，提示"X已记住阵型"） |
| T10 | 完成一次每日挑战 | +20（首胜 +10），当日按钮置灰；次日规则轮换 |
| T11 | 达成「田忌传人」「绝地翻盘」等 | 解锁 Toast + 成就弹窗内点亮 |
| T12 | 刷新页面 | 段位/声望/战报/成就完整恢复 |
| T13 | 关闭音效后操作 | 全程无声；再开恢复 |

---

## 12. 迭代计划

| 阶段 | 内容 | 验收口径 |
| --- | --- | --- |
| **P0（本 PRD 评审通过后）** | 按 6.2 策略实施单屏改造：`#stage` 画布 + 缩放、备战/结算改覆盖层、删除滚动调用；回归全部玩法 | 通过 11.1 全部阻断级用例 + 11.2 回归 |
| **P1** | 本地双人热座模式（102 vs 201 同设备秘密布阵）；主页数据条加 [👥 双人对战] 入口 | 双人完整一局无歧义；共享现有判定逻辑 |
| **P2** | 战绩分享卡片（Canvas 生成图片 / 复制战绩文本）；宽屏双栏增强布局（左对阵表、右赛道） | 分享图可读可传播；宽屏下依旧无滚动 |

---

*（附：本 PRD 描述的玩法与成长系统大部分已在现有 `index.html` / `style.css` / `game.js` 中实现；P0 的目标是让它们满足"单屏 · 无滚动"硬约束，差距清单见 6.3。）*
