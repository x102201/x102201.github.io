# x102201.github.io 项目文档（PRD）

## 1. 背景与目标

### 1.1 背景

`x102201.github.io` 为个人 GitHub Pages 站点。本规格用于重建站点：从「单页应用 / 游戏演示」转向「个人仓库橱窗」，用于介绍并导流到各 GitHub 仓库。

### 1.2 目标

- 用固定、清晰的站点模式展示多个项目。
- 每个项目的首页横幅与详情页按项目单独精细化设计，不套统一内容模板。
- 同时支持手机端与电脑端。
- **中英文双语各做一版页面；语言判定为偏好 > 浏览器语言 > 默认英文。**
- 纯静态托管于 GitHub Pages，零构建依赖。

### 1.3 设计原则

**走廊统一，展厅各自设计。**

- 统一：导航、跳转路径、顶栏内容导航、语言入口、响应式基线。
- 不统一：横幅版式与文案、详情页结构与视觉（中英文版可各自排版，不要求 DOM 一一对应）。

---

## 2. 用户路径

```text
访问站点
  → 按「偏好 > 浏览器语言 > 默认英文」进入对应语言首页
  → 浏览纵向排列的项目横幅（顶栏内容导航随滚动高亮当前横幅）
  → 也可在顶栏直接点击项目名，快速滚动到对应横幅
  → 点击某一横幅
  → 进入该项目对应语言的独立详情页
  → 阅读介绍后点击「View repo」/「查看仓库」跳转 GitHub
  → 或点击返回回到同语言首页
  → 可随时通过顶栏切换 EN / 中文（写入偏好，并跳到对应语言的平行页面）
```

核心转化目标：访客理解项目 → 进入对应 GitHub 仓库。

---

## 3. 信息架构与 URL

### 3.1 语言策略（已定）

| 项 | 约定 |
|----|------|
| 模式 | **双语各做一版**（独立 HTML，不是同一页切文案） |
| 默认语言 | **英文（en）**（优先级最低的兜底） |
| 中文 | 平行页面，路径带 `/zh/` 前缀（站点级）或详情下的 `zh/`（见下表） |
| 切换 | 顶栏提供 EN / 中文；切换时写入 `localStorage` 的 `lang`（`en` \| `zh`） |
| 语言判定优先级 | **已存偏好（`localStorage.lang`）> 浏览器语言 > 默认英文** |

**语言判定细则：**

1. 若存在有效的 `localStorage.lang`（`en` 或 `zh`），一律按偏好进入对应语言页面。
2. 若无已存偏好：读取 `navigator.language`（及可用的 `navigator.languages`）；以 `zh` 开头则视为中文，否则视为英文。
3. 仍无法判断时，使用默认英文。
4. 访问站点根路径 `/` 时，按上述结果必要时重定向到 `/zh/`（判定为中文且当前在英文首页时）；从中文站进入英文同理。直接打开某一语言的详情深链时，以该 URL 为准展示，可将当前语言写入偏好，避免之后来回跳。

不采用：单页 `data-i18n` 文案替换作为主方案。

### 3.2 页面关系

```text
index.html (en)  --横幅-->  projects/{id}/detail/           --仓库--> GitHub
     ^                         |
     |-------- 返回 -----------|

zh/index.html    --横幅-->  projects/{id}/detail/zh/       --仓库--> GitHub
     ^                         |
     |-------- 返回 -----------|

EN 首页 ←—— 语言切换 ——→ 中文首页
EN 详情 ←—— 语言切换 ——→ 中文详情（同一项目）
```

### 3.3 URL 约定

| 页面 | 英文（默认） | 中文 |
|------|--------------|------|
| 首页 | `/` 或 `/index.html` | `/zh/` 或 `/zh/index.html` |
| 项目详情 | `/projects/{id}/detail/` | `/projects/{id}/detail/zh/` |
| 仓库 | 外链至对应 GitHub 仓库（中英可相同） | 同左 |

### 3.4 明确不采用

- 不使用 `project.html?id=xxx` 的查询参数路由。
- 不为所有项目共用同一套详情字段模板页。
- 不以「同一 HTML 动态替换文案」作为双语主方案。

---

## 4. 目录与命名约定

### 4.1 目录结构

```text
/
├── index.html                 # 英文首页（默认）
├── zh/
│   └── index.html             # 中文首页
├── css/
│   └── base.css               # 全站：重置、字体、断点、顶栏、链接、语言切换基线
├── js/
│   ├── projects.js            # 登记项目顺序、标题与中英入口路径
│   ├── lang.js                # 语言判定、偏好读写、首页重定向
│   └── nav.js                 # 顶栏内容导航：首页滚动联动 / 详情页固定当前项
├── projects/
│   └── {id}/
│       ├── banner/
│       │   ├── en/            # 英文横幅源（HTML / CSS / 资源）
│       │   └── zh/            # 中文横幅源
│       ├── particles.js       # 可选：项目级粒子背景（勿放全站 js/）
│       ├── particles.css      # 可选：粒子层样式（勿覆盖顶栏 sticky）
│       └── detail/
│           ├── index.html     # 英文详情（默认 URL）
│           ├── detail.css     # 可中英共用或再拆
│           ├── detail.js      # 可选：详情页滚动显现、复制等交互
│           └── zh/
│               └── index.html # 中文详情
├── assets/                    # 可选：全站共用资源
└── PRD.md                     # 本规格文档
```

### 4.2 `{id}` 命名

- 使用稳定、小写、短横线分隔的英文标识，例如：`card-duel`、`tianji`。
- `{id}` 一经上线尽量不改，避免外链失效。

### 4.3 `banner/` 与 `detail/` 职责

| 目录 | 职责 | 是否对外分享 |
|------|------|----------------|
| `banner/en/`、`banner/zh/` | 对应语言首页横幅的设计源与专用资源 | 不强制；一般不作为正式分享入口 |
| `detail/`（英）、`detail/zh/`（中） | 对应语言的项目介绍页；必须可回**同语言**首页，且有仓库链接 | 是；正式 URL |

中英文详情可以共享部分 CSS/图片，但 **HTML 必须各有一版**，以便版式与文案独立精细化。

### 4.4 首页接入横幅的方式

- 首期：英文定稿横幅写入根目录 `index.html`；中文定稿横幅写入 `zh/index.html`。
- `projects/{id}/banner/en/` 与 `banner/zh/` 作为各自语言横幅的源目录，精修后再同步到对应首页。

---

## 5. 页面职责

### 5.1 顶栏（全站固定结构）

顶栏从左到右分为三块：

| 区域 | 职责 |
|------|------|
| 站点名 | 链回**当前语言**首页（`/` 或 `/zh/`） |
| **内容导航** | 展示全部已上架项目，标示当前所处内容，支持快速切换（见 5.1.1） |
| 语言切换 | **EN / 中文** pill 分段控件；切换时写入 `localStorage.lang` 并跳转平行页 |

顶栏在首页与详情页均**固定吸顶**（`site-nav-shell` 使用 `position: sticky`），滚动时不离开视口。

#### 5.1.1 内容导航（Content Nav）

**用途：** 项目增多时，访客始终知道「当前在看哪个」，并可在顶栏一键跳到其他项目。

**数据来源：** `js/projects.js` 中的项目顺序与 `titleEn` / `titleZh`；首页横幅 DOM 需带与 `id` 对应的锚点（如 `id="banner-{id}"` 或 `data-project-id="{id}"`）。

**首页行为（随滚动切换）：**

- 列出当前语言下全部项目的短标题（可横向滚动，避免项目过多时挤爆顶栏）。
- **滚动联动：** 用户滚动横幅列表时，根据视口内当前主横幅更新高亮项（推荐 `Intersection Observer`，阈值以「占据视口面积最大的一条横幅」为准）。
- **点击跳转：** 点击某一项目名，页面平滑滚动到对应横幅锚点；更新 URL hash（如 `#banner-{id}`）以便分享与刷新定位。
- 进入首页时若 URL 带 hash，滚动到对应横幅并将该项标为当前。

**详情页行为（不随滚动切换）：**

- 同样列出全部项目，但**当前项固定为 URL 所对应的项目**，详情页内向下滚动**不改变**高亮项。
- 点击其他项目名：跳转到该项目的**同语言**详情页（非首页锚点）。
- 点击当前项目名：无操作或轻微反馈（避免误触重复跳转）。

**视觉：**

- 当前项需明显区分（高亮、字重、浅底 pill 等）；非当前项可弱化但仍可点击。
- 手机端：内容导航区域可横向滑动；单项触控区域 ≥ 44px。

#### 5.1.2 顶栏视觉与实现（参考 DeepSeek Harness）

**结构（写入 `css/base.css`，全站一致）：**

```text
site-nav-shell          # sticky 外层，顶部留白，pointer-events: none
└── site-nav            # 胶囊形浮动条（max-width 与内容区对齐）
    ├── site-nav__brand
    ├── site-nav__content   # 内容导航（由 nav.js 注入）
    └── site-lang           # pill 语言切换
```

**布局顺序：** 站点名 → **内容导航（紧挨左侧，顺序与 `projects.js` / 首页横幅一致）** → 语言切换靠右（`margin-left: auto`）。项目增多时，中间导航区横向滚动，语言切换不被挤出。

**滚动行为：**

- 页面顶部：顶栏**完全透明**，无 `backdrop-filter`（避免空白区域「糊一块」）。
- 滚动后（`nav.js` 为 `.site-nav` 加 `.is-scrolled`）：通过 `::before` 伪元素淡入轻量玻璃态（背景 + blur + 细边框），**不要**在默认态给整条顶栏加 blur。

**注意：**

- 项目级 `particles.css` **不得**将 `.site-nav` 设为 `position: relative`，否则会覆盖 sticky 失效。
- 顶栏高度宜紧凑（约 40px 栏高 + 8px 顶留白），避免占过多视口。

### 5.2 首页（`index.html` / `zh/index.html`）

- 顶栏按 5.1 节：站点名 + 内容导航 + 语言切换。
- 主体：纵向一列全宽项目横幅（仅展示当前语言版）；每条横幅带与 `projects.js` 一致的 `id` 锚点，供内容导航滚动联动。
- 每条横幅点击进入**同语言**详情：`projects/{id}/detail/` 或 `projects/{id}/detail/zh/`。
- 模式固定；每条横幅内容与版式按项目、按语言单独设计。

### 5.3 横幅 `projects/{id}/banner/en|zh/`

- 仅服务对应语言首页的展示与导流。
- 可自有样式与资源；class 建议带项目前缀（如 `.banner-card-duel`）。
- 每种语言横幅都需考虑手机与桌面布局。
- 若需粒子背景：**canvas 必须放在横幅容器内部**（如 `#banner-{id}` 的首个子元素），横幅背景勿用不透明实色盖住 canvas；首页在 `index.html` / `zh/index.html` 引入该项目的 `particles.js` / `particles.css`。

### 5.4 详情 `projects/{id}/detail/` 与 `detail/zh/`

- 每项目、每语言独立页面；布局与章节可不同。
- 顶栏按 5.1 节：内容导航**固定标示当前项目**，详情页内滚动**不触发**导航项切换。
- **全站仅要求：**
  1. 返回**同语言**首页（英 → `/`，中 → `/zh/`）；可由站点名或页内返回入口承担；
  2. 至少一个明确的仓库外链（英文可用 “View repository”，中文可用「查看仓库」）；
  3. 顶栏或页内提供切到**同一项目另一语言详情**的入口；
  4. 引入 `css/base.css` 与 `js/nav.js`（详情模式），可用自有样式覆盖；
  5. 手机与电脑均可正常阅读与操作。

**可选交互（`detail.js` + `detail.css`，首期 DSHHelper 已采用）：**

- 区块滚动显现（`Intersection Observer`）；`prefers-reduced-motion: reduce` 时跳过动画。
- 截图 / 卡片 hover 反馈；步骤区编号与高亮。
- Releases 等外链可做成「点击复制」按钮。
- 引入顺序建议：`projects.js` → `nav.js` → `detail.js` → 项目 `particles.js`（若有）。

**项目级粒子（可选，非全站）：**

- 文件位于 `projects/{id}/particles.js`、`particles.css`；详情页 canvas 挂于 `<body>` 下，须 `pointer-events: none`。
- 鼠标互动**仅**作用于 Hero / 横幅文案区，**不要**绑定全页或截图区域，以免拖慢图片 hover / 点击。
- `particles.css` 仅提升 `main` / `.site-main` 的层级，**勿**改动顶栏定位。
- 尊重 `prefers-reduced-motion`：应移除 canvas 或停用动画。

---

## 6. 设计与响应式规范

### 6.1 固定与不固定对照

| 固定（模式） | 不固定（内容） |
|--------------|----------------|
| 首页纵向全宽横幅列表 | 每个横幅的版式、文案、视觉（含中英差异） |
| 横幅点击进入同语言 `detail` | 每个详情页的章节与布局（中英可不同） |
| 顶栏三块：站点名 / 内容导航 / 语言切换 | 配色、字体覆盖、动效、素材 |
| 内容导航：首页随滚动高亮，详情固定当前项 | 项目内 banner / detail 样式与动效 |
| 顶栏胶囊条 + 滚动后玻璃态（§5.1.2） | 是否使用项目级粒子 / detail.js |
| `base.css` 断点与安全边距 | 各页具体文案 |
| 双语平行页；语言优先级：偏好 > 浏览器 > 英文 | — |

### 6.2 响应式基线（写入 `base.css`，各页遵守）

- 手机优先；左右安全边距 ≥ 16–20px。
- `≥768px`：横幅与详情允许左右分栏等更宽布局。
- 内容区在大屏可限制最大宽度并居中；横幅仍可为全宽视觉平面。
- 主按钮 / 主点击区适合触控（建议可点高度 ≥ 44px）。

### 6.3 双端与双语验收清单

每个项目的 **en + zh** 横幅与详情均需检查：

- 文字不溢出、不贴边。
- 窄屏上仍能一眼看懂项目是什么。
- 仓库链接易于发现。
- 横幅点击、返回、**语言切换**在手机与桌面均可用，且切换后仍停留在同一项目的平行页（详情）或对应语言首页。
- **内容导航：** 首页滚动时高亮随当前横幅变化；详情页滚动时高亮不变；顶栏点击可正确跳转（首页 → 锚点滚动，详情 → 同语言其他项目详情）。
- **顶栏：** 滚动前后透明 / 玻璃态切换正常；内容导航在站点名右侧、顺序与横幅一致。
- **动效（若启用）：** 粒子不挡点击；减少动态效果系统下无粒子或无不必要动画。

---

## 7. 数据与维护流程

### 7.1 `js/projects.js`

仅作目录登记。建议字段包括：

- `id`：与目录名一致；用于横幅锚点 `banner-{id}` 与内容导航 `data-project-id`
- `titleEn` / `titleZh`：横幅与顶栏内容导航的短标题（过长时 CSS 截断，完整名可放在 `title` 属性）
- `detailPathEn`：如 `projects/{id}/detail/`
- `detailPathZh`：如 `projects/{id}/detail/zh/`

数组顺序即首页横幅展示顺序与顶栏内容导航顺序。不以该文件生成横幅/详情 HTML 模板，但 **`nav.js` 依赖此文件渲染顶栏导航列表**。

### 7.2 上新项目流程

1. 新建 `projects/{id}/banner/en/`、`banner/zh/` 与 `detail/`、`detail/zh/`。
2. 分别精细化设计英文、中文的横幅与详情（含手机与桌面）。
3. 将英文横幅挂到 `index.html`，中文横幅挂到 `zh/index.html`，每条横幅设置锚点 `id="banner-{id}"`，链接到各自详情路径。
4. 在 `js/projects.js` 登记 `id`、中英标题与路径。
5. 确认顶栏内容导航能列出新项目；首页滚动联动与详情页固定当前项行为正常。
6. 若使用粒子：确认 canvas 在横幅内或详情 body 下、`pointer-events: none`、顶栏 sticky 未被覆盖。
7. 按第 6.3 节做双端 + 双语检查。

### 7.3 首批上架项目

| id | 标题（EN） | 标题（ZH） | 仓库 | 状态 |
|----|------------|------------|------|------|
| `dsh-helper` | DSHHelper | DSHHelper | https://github.com/x102201/deepseek-harness-helper | 已上架 |

---

## 8. 首期范围与后续扩展

### 8.1 首期范围（做）

- 按本文档建立目录与 `base.css` / 中英首页骨架。
- 支持多项目：各自 `banner/en|zh` + `detail` + `detail/zh`。
- 双语平行站；顶栏含内容导航（首页滚动联动、详情固定当前项）与语言切换；语言判定为 **偏好 > 浏览器语言 > 默认英文**。
- 响应式可用；详情可回同语言首页并链到仓库。

### 8.2 首期不做

- 统一详情字段模板、CMS、打包构建工具。
- 单页动态文案 i18n 作为双语主方案。
- 评论系统、复杂仪表盘式首页。
- 查询参数路由（`?id=`）。

### 8.3 后续可扩展（非首期必做）

- 项目增多后，用脚本从 `banner/en|zh` 同步到对应首页。
- 在 `projects.js` 基础上增加简单过滤或分组。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-27 | 初版：橱窗站定位、独立详情页、`banner`/`detail` 目录与响应式约定 |
| 2026-08-27 | 增补双语：中英各做一版页面；默认英文；`/zh/` 与 `detail/zh/` 平行路径 |
| 2026-08-27 | 语言判定改为：已存偏好 > 浏览器语言 > 默认英文 |
| 2026-08-27 | 首批上架 `dsh-helper`（DSHHelper）及仓库地址 |
| 2026-08-27 | 顶栏增加内容导航：首页随滚动切换高亮，详情页固定当前项目 |
| 2026-08-27 | 顶栏改为 Harness 式胶囊条；补充项目级粒子、detail.js 与实现注意事项 |
